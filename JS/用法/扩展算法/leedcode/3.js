/**
 * ========================================
 * LeetCode 3. 无重复字符的最长子串
 * ========================================
 * 
 * 【评分】⭐ (1/5)
 * 
 * 【你的思路】
 * 文件中只有标题和注释，没有实现代码
 * 
 * 【正确思路】
 * 滑动窗口 + Set/Map：
 * 1. 使用双指针维护一个滑动窗口
 * 2. 右指针不断扩展窗口
 * 3. 遇到重复字符时，左指针收缩窗口
 * 4. 始终保持窗口内无重复字符
 * 
 * 【参考实现】
 * ```javascript
 * var lengthOfLongestSubstring = function(s) {
 *     let set = new Set();
 *     let left = 0, maxLen = 0;
 *     
 *     for (let right = 0; right < s.length; right++) {
 *         while (set.has(s[right])) {
 *             set.delete(s[left]);
 *             left++;
 *         }
 *         set.add(s[right]);
 *         maxLen = Math.max(maxLen, right - left + 1);
 *     }
 *     return maxLen;
 * };
 * ```
 * 
 * 【常见错误】
 * ❌ 暴力枚举所有子串 - O(n³)超时
 * ❌ 只用单层循环，没有正确处理重复
 * 
 * 【复杂度】
 * 时间：O(n) - 每个字符最多被访问两次
 * 空间：O(min(n, 字符集大小))
 * 
 * 【面试建议】
 * 滑动窗口是解决子串/子数组问题的核心技巧：
 * - 窗口扩展：右指针移动
 * - 窗口收缩：左指针移动
 * - 用Set/Map记录窗口内元素
 * ========================================
 */

//无重复字符的最长子串
// 三、无重复最长子串（文档 3）
// 现状
// 文档里只有标题，没有代码
// 常见错误：暴力枚举所有子串（O(n³)）
// 优化思路 ✅
// 滑动窗口 + Set / Map
// 核心思想
// 右指针不断扩展
// 出现重复字符，左指针收缩
// 始终维护一个"无重复窗口"
// 复杂度
// 时间：O(n)
// 空间：O(min(n, 字符集大小))
