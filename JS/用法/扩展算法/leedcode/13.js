/**
 * ========================================
 * LeetCode 13. 罗马数字转整数
 * ========================================
 * 
 * 【评分】⭐ (0/5)
 * 
 * 【你的思路】
 * 文件为空，没有实现代码
 * 
 * 【正确思路】
 * 从左到右遍历，比较当前字符与下一字符：
 * 1. 如果当前字符值 < 下一字符值，说明是特殊规则（如IV、IX）
 *    - 减去当前值
 * 2. 否则
 *    - 加上当前值
 * 
 * 【参考实现】
 * ```javascript
 * var romanToInt = function(s) {
 *     const map = {
 *         'I': 1, 'V': 5, 'X': 10, 'L': 50,
 *         'C': 100, 'D': 500, 'M': 1000
 *     };
 *     
 *     let result = 0;
 *     for (let i = 0; i < s.length; i++) {
 *         const curr = map[s[i]];
 *         const next = map[s[i + 1]];
 *         
 *         if (next && curr < next) {
 *             result -= curr;
 *         } else {
 *             result += curr;
 *         }
 *     }
 *     return result;
 * };
 * ```
 * 
 * 【关键点】
 * - 罗马数字规则：小值在大值左边表示减法
 * - 如：IV = 5-1 = 4, IX = 10-1 = 9
 * - 从左到右遍历，遇到递减就减，否则加
 * 
 * 【复杂度】
 * 时间：O(n)
 * 空间：O(1)
 * 
 * 【面试建议】
 * 这题是12题的逆过程，
 * 掌握映射表和比较逻辑即可。
 * ========================================
 */
